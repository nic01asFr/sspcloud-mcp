apiVersion: v1
kind: Service
metadata:
  name: {{ include "sspcloud-mcp.fullname" . }}
  labels:
    {{- include "sspcloud-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "sspcloud-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
